let x = 5;
let name = "Steven";
let passing = x > 3;

let test = 5 + 12;

if (passing) {
    let bonus = (x + 2) * (2/1);
    print(bonus);
} else {
    print(x);
}

fn add(a, b) {
    let result = a + b;
    return result;
}


let total = add(x, 10);
print(total);
